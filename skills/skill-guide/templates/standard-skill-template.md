---
name: {skill-name}
description: '{WHAT this skill does}. Use when {TRIGGER SCENARIO 1}, {TRIGGER SCENARIO 2},
  {TRIGGER SCENARIO 3}, or {TRIGGER SCENARIO 4}. Covers {TOPIC 1}, {TOPIC 2}, and {TOPIC 3}.'
license: MIT
compatibility: '{Required tools or environment, e.g., Node.js 18+, gh CLI authenticated}'
---

# {Skill Title}

{One-paragraph overview: what this skill does, what problem it solves, and when it is the right tool.}

## When to Use This Skill

Use this skill when:

- User asks to "{trigger phrase 1}"
- User needs to "{trigger phrase 2}"
- User mentions error: "{common error or symptom}"
- User wants to {use case involving bundled references}

## Prerequisites

- {Required tool / runtime, e.g., Python 3.11+}
- {Required CLI, e.g., Azure CLI (`az`) authenticated}
- {Optional dependency and what it enables}

## Core Workflow

### Step 1: {Action Name}

{Description of what to do and why.}

```bash
# Command with inline explanation
{command}
```

> For additional options, see [references/{reference-file}.md](references/{reference-file}.md).
> Search patterns: `{keyword1}`, `{keyword2}`, `{keyword3}`

---

### Step 2: {Action Name}

{Description of what to do.}

| Option | Default | Description |
|--------|---------|-------------|
| `{option1}` | `{default}` | {What it controls} |
| `{option2}` | `{default}` | {What it controls} |

---

### Step 3: {Action Name}

{Description of what to do.}

```{language}
{code example}
```

## Common Patterns

### Pattern: {Pattern Name}

{When to use this pattern.}

```{language}
{code}
```

### Pattern: {Pattern Name}

{When to use this pattern.}

```{language}
{code}
```

## Troubleshooting

| Problem | Solution |
|---------|----------|
| {Common error message} | {Quick fix} |
| {Another error} | {Quick fix or reference link} |

> For comprehensive troubleshooting: see [references/troubleshooting.md](references/troubleshooting.md)
> Search patterns: `{error keyword 1}`, `{error keyword 2}`

## References

- [references/{file1}.md](references/{file1}.md) — {When to read it; search patterns}
- [references/{file2}.md](references/{file2}.md) — {When to read it; search patterns}
